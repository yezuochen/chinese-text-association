# GraphRAG — LLM calls via NVIDIA NIM (free, 40 RPM)
NIM_API_KEY=nvapi-...
NIM_BASE_URL=https://integrate.api.nvidia.com/v1

# GraphRAG internal embeddings via OpenAI (~US$0.05 for 100 files)
# Routed to OpenAI to sidestep NIM's `encoding_format: null` rejection
OPENAI_API_KEY=sk-...